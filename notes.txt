git checkout -- config/contract_addresses.json
git checkout -b separated-projects origin/separated-projects
???

In different terminals:
geth --dev --http --http.addr 127.0.0.1 --http.port 8545 --http.api eth,web3,personal,net

cd Blockchain/
    truffle compile 
        (compile .sol contracts into .json and place them in Blockchain/build/contracts)
    truffle migrate

in /Carboni_Thesis
    python3 -m Org1.main
python main.py

pipreqs --force --ignore .venv,.history
pip install -r requirements.txt
