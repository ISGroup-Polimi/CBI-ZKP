PR = Private
PUB = Public
C = Converted

    # Virtual env on/off
source .venv/bin/activate
deactivate

    ### Project
pip install -r requirements.txt

In different terminals:
geth --dev --http --http.addr 127.0.0.1 --http.port 8545 --http.api eth,web3,personal,net

cd Carboni_Thesis/Blockchain
    truffle compile 
        (compile .sol contracts into .json and place them in Blockchain/build/contracts)
    truffle migrate
        (execute migrations script: 1_deploy_contracts.js and 2_deploy_contracts.js
         publish contract_addresses.json in Blockchain folder)

in /Carboni_Thesis
    python3 -m Org1.main
    python3 -m OrgB.main


    # commit from VM
git add .
git commit -m "Show results"
git push


# create branch
git checkout -b branch_name

# go to branch
git checkout "branch-name"


Add swap (sort of virtual memory)
    sudo fallocate -l 8G /swapfile
    sudo chmod 600 /swapfile
    sudo mkswap /swapfile
    sudo swapon /swapfile


# See which is the size of files to push
git ls-files --stage | awk '{print $4}' | xargs -I{} du -h {} | sort -hr